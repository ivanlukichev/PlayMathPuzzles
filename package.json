{
  "name": "example-com",
  "private": true,
  "scripts": {
    "build": "python3 scripts/prepare_public_dir.py",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  }
}
